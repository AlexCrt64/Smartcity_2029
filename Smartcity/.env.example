## Exemple de variables d'environnement pour le projet Smartcity
# Copier ce fichier en .env puis remplacer les valeurs.
# Ne JAMAIS commiter le fichier .env.

# Clé API exposée au client (si elle ne permet que des opérations publiques / quota limité)
# Toute variable commençant par EXPO_PUBLIC_ sera intégrée dans le bundle et donc visible.
EXPO_PUBLIC_API_KEY=VOTRE_CLE_API_ICI

# NOTE IMPORTANTE : Si cette clé donne accès à des opérations sensibles ou sans restrictions,
# mettez en place un backend proxy et ne la mettez PAS côté client.
